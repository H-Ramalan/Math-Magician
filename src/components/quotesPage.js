import React from 'react';
import Nav from './nav';
import Quote from './Quote';

const QuotePage = () => (
  <div>
    <Nav />
    <main>
      <Quote />
    </main>
  </div>
);

export default QuotePage;
